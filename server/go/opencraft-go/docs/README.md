<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# opencraft\-go

```go
import "github.com/jdonkervliet/opencraft-go/cmd/opencraft-go"
```

## Index

- [Variables](<#variables>)
- [func main\(\)](<#main>)
- [type GameData](<#GameData>)


## Variables

<a name="blocks"></a>

```go
var blocks string
```

<a name="main"></a>
## func [main](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/cmd/opencraft-go/main.go#L31>)

```go
func main()
```



<a name="GameData"></a>
## type [GameData](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/cmd/opencraft-go/main.go#L21-L29>)

GameData represents the types of objects that be present in the game such as blocks and items. These can be specified in yaml format to make them easy to change.

```go
type GameData struct {
    Blocks []struct {
        Name  string `yaml:"name"`
        Color string `yaml:"color"`
    }   `yaml:"blocks"`
    Items []struct {
        Name string `yaml:"name"`
    }   `yaml:"items"`
}
```

# server

```go
import "github.com/jdonkervliet/opencraft-go/server"
```

## Index

- [type Game](<#Game>)
  - [func NewGame\(\) \*Game](<#NewGame>)
  - [func \(g \*Game\) HandleMessages\(\)](<#Game.HandleMessages>)
  - [func \(g \*Game\) IsRunning\(\) bool](<#Game.IsRunning>)
  - [func \(g \*Game\) Start\(\) error](<#Game.Start>)
  - [func \(g \*Game\) Stop\(\)](<#Game.Stop>)
  - [func \(g \*Game\) Update\(\)](<#Game.Update>)
  - [func \(g \*Game\) handleIWantChangeBlock\(msg \*protos.IWantChangeBlock, conn net.Conn\)](<#Game.handleIWantChangeBlock>)
  - [func \(g \*Game\) handleIWantColumn\(msg \*protos.IWantColumn, conn net.Conn\)](<#Game.handleIWantColumn>)
  - [func \(g \*Game\) handleIWantMovePlayer\(msg \*protos.IWantMovePlayer, conn net.Conn\)](<#Game.handleIWantMovePlayer>)
  - [func \(g \*Game\) handleIWantPlayer\(msg \*protos.IWantPlayer, conn net.Conn\)](<#Game.handleIWantPlayer>)
  - [func \(g \*Game\) handleMessage\(msg \*IncomingMessage\)](<#Game.handleMessage>)
- [type IncomingMessage](<#IncomingMessage>)
- [type ServerPlayer](<#ServerPlayer>)
  - [func newServerPlayer\(\) \*ServerPlayer](<#newServerPlayer>)


<a name="Game"></a>
## type [Game](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L44-L52>)

Game is the root struct of a single game instance.

```go
type Game struct {
    World        model.World
    Players      map[uint32]*ServerPlayer
    TickDuration time.Duration

    running bool
    msgBuf  chan *IncomingMessage
    s       net.Listener
}
```

<a name="NewGame"></a>
### func [NewGame](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L55>)

```go
func NewGame() *Game
```

The default constructor for the Game struct.

<a name="Game.HandleMessages"></a>
### func \(\*Game\) [HandleMessages](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L197>)

```go
func (g *Game) HandleMessages()
```

Handles incoming messages from clients. This method first checks the length of the message queue and then processes exactly that number of messages. As such, the method is guaranteed to terminate, even when the incoming message rate exceeds the message processing rate.

<a name="Game.IsRunning"></a>
### func \(\*Game\) [IsRunning](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L69>)

```go
func (g *Game) IsRunning() bool
```

Returns true iff the game is running and has not been instructed to stop.

<a name="Game.Start"></a>
### func \(\*Game\) [Start](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L209>)

```go
func (g *Game) Start() error
```

Starts the game server. Specifically, starts listening on a socket for incoming client messages and enqueues them. This function does not start simulating the game world and does not process incoming messages, that needs to be done seperately by the caller of this function by calling Game.Update\(\).

<a name="Game.Stop"></a>
### func \(\*Game\) [Stop](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L74>)

```go
func (g *Game) Stop()
```

Stops the game. The game stops sending and receiving messages.

<a name="Game.Update"></a>
### func \(\*Game\) [Update](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L64>)

```go
func (g *Game) Update()
```

Updates the game by one step.

<a name="Game.handleIWantChangeBlock"></a>
### func \(\*Game\) [handleIWantChangeBlock](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L139>)

```go
func (g *Game) handleIWantChangeBlock(msg *protos.IWantChangeBlock, conn net.Conn)
```

Handles the IWantChangeBlock message from the client. Tries to set the block at the specified location to the specified type. It does not send a reply.

<a name="Game.handleIWantColumn"></a>
### func \(\*Game\) [handleIWantColumn](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L153>)

```go
func (g *Game) handleIWantColumn(msg *protos.IWantColumn, conn net.Conn)
```

Handles the IWantColumn message from the client. Currently, the game does not keep track of the world and simply generates a column with a flat 1\-block thick layer of non\-air blocks in a ColumnData message. TODO the game should generate and keep track of the world.

<a name="Game.handleIWantMovePlayer"></a>
### func \(\*Game\) [handleIWantMovePlayer](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L127>)

```go
func (g *Game) handleIWantMovePlayer(msg *protos.IWantMovePlayer, conn net.Conn)
```

Handles the IWantMovePlayer message from the client. This moves a player avatar to a new position in the world. Currently, the game performs no checks whatsoever if this move is valid. It simply accepts the new position. It does not send a reply.

<a name="Game.handleIWantPlayer"></a>
### func \(\*Game\) [handleIWantPlayer](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L85>)

```go
func (g *Game) handleIWantPlayer(msg *protos.IWantPlayer, conn net.Conn)
```

Handles the IWantPlayer message sent by a client. This message is sent when a client initially connects to the server and wants to log in. The client can request to log in with a specific player ID by sending a value \> 0. If the received player ID is 0, the game automatically assigns a player ID. Clients logged in with the same player ID control the same player/avatar. On a successful login, the server replies to the client with a YouArePlayer message telling the client their player ID and avatar location.

<a name="Game.handleMessage"></a>
### func \(\*Game\) [handleMessage](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L177>)

```go
func (g *Game) handleMessage(msg *IncomingMessage)
```

Handles a single incoming message from a client and sends a reply if necessary.

<a name="IncomingMessage"></a>
## type [IncomingMessage](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L18-L21>)

IncomingMessage is a struct used to wrap messages that come in from clients. In includes the client connection \(to enable sending a reply\) and the original protobuf message.

```go
type IncomingMessage struct {
    Connection net.Conn
    Data       *protos.ToServer
}
```

<a name="ServerPlayer"></a>
## type [ServerPlayer](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L29-L32>)

ServerPlayer represents a player on the server. It contains the data associated with the player's avatar and a slice of connections. We allow a slice of connections, instead of a single connection, to enable a player to log simultaneously via multiple connections to facilitate easy handover when a player switches connection, for example when switching between a regular client to a thin client.

```go
type ServerPlayer struct {
    model.Player
    controllers []net.Conn
}
```

<a name="newServerPlayer"></a>
### func [newServerPlayer](<https://github.com/jdonkervliet/opencraft/blob/master/server/go/opencraft-go/server/server.go#L35>)

```go
func newServerPlayer() *ServerPlayer
```

The default constructor for the ServerPlayer struct.

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
