
# Opencraft Go Server

Install the latest version of Go. A simple `go build` or `go install` should be
sufficient to obtain a binary version of the server compatible with your
platform. Simply start the server by running the binary. The server listens for
client connections over TCP on port 7979.
